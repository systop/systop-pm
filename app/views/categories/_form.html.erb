<%= form_with(model: category, local: true, class: "form-horizontal") do |form| %>
<%# form_for(@category, url: project_categories_path) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- <div class="form-inline"> -->
    <div class="form-group">
      <%= form.label :title, class: "col-sm-2 control-label" %>
      <div class="col-sm-2">
        <%= form.text_field :title, id: :category_title, class: "form-control" %>
      </div>
    </div>
  <!-- </div> -->

  <!-- <div class="form-group" hidden=true> -->
  <div class="form-group">
    <%= form.label :project_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-2">
      <%= form.text_field :project_id, id: :category_project_id, value: params[:project_id], class: "form-control", readonly: true %>
    </div>
  </div>


  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= form.submit  class: "btn btn-primary"%>
    </div>
  </div>
<% end %>
  <%#params%>
